import React, { FC } from "react";
import {
  Button,
  Card,
  TextField,
  Typography,
  FormControl,
  Grid,
  InputLabel,
  MenuItem,
} from "@mui/material";

const VendorSingleProjectView: FC = () => {

  return (
    <>
      <Card sx={{ margin: "2%" }}>
        <h1>this is the single project dashboard</h1>
      </Card>
    </>
  );
};

export default VendorSingleProjectView;
